CREATE DATABASE UniversityЗадачаs;

-- Задача 1:

CREATE TABLE Author (
    AuthorID INT PRIMARY KEY AUTO_INCREMENT,
    FirstName VARCHAR(50),
    LastName VARCHAR(50)
);

CREATE TABLE Publisher (
    PublisherID INT PRIMARY KEY AUTO_INCREMENT,
    Name VARCHAR(100)
);

CREATE TABLE Book (
    BookID INT PRIMARY KEY AUTO_INCREMENT,
    Title VARCHAR(100),
    Price DECIMAL(6,2),
    AuthorID INT,
    PublisherID INT,
    Country VARCHAR(100),
    FOREIGN KEY (AuthorID) REFERENCES Author(AuthorID) ON DELETE CASCADE,
    FOREIGN KEY (PublisherID) REFERENCES Publisher(PublisherID)
);

-- Задача 2:
INSERT INTO Author (FirstName, LastName) VALUES
('Ivan', 'Petrov'),
('Maria', 'Ivanova'),
('Georgi', 'Georgiev');

INSERT INTO Publisher (Name) VALUES
('Ciela'),
('Hermes'),
('SoftPress');

INSERT INTO Book (Title, Price, AuthorID, PublisherID, Country) VALUES
('Python Basics', 25.00, 1, 1, 'Bulgaria'),
('MySQL Essentials', 30.00, 2, 2, 'Romania'),
('Data Science Intro', 22.50, 3, 3, 'Azerbaijan');

-- Задача 3:
UPDATE Book SET Price = Price + 5 WHERE Title = 'Python Basics';
DELETE FROM Book WHERE Title = 'Data Science Intro';

-- Задача 4:
SELECT * FROM Book WHERE Price BETWEEN 20 AND 35;

-- Задача 5: 
CREATE TABLE Readers 
(
    ReaderID INT PRIMARY KEY AUTO_INCREMENT,
    FirstName VARCHAR(50),
    LastName VARCHAR(50),
    Gender CHAR(1)
);

INSERT INTO Readers (FirstName, LastName, Gender) VALUES
('Анна', 'Петрова', 'F'),
('Георги', 'Георгиев', 'M'),
('Мария', 'Иванова', 'F'),
('Иван', 'Георгиев', 'M');

SELECT * FROM Readers WHERE Gender = 'F';


-- Задача 6: 
SELECT * FROM Readers WHERE FirstName = 'Георги' OR LastName = 'Георгиев';

-- 10.03.2026 Упражнение 3
-- Задача 1:
CREATE TABLE BookReader 
(
    ID INT PRIMARY KEY AUTO_INCREMENT,
    ReaderID INT,
    BookID INT,
    DateTaken VARCHAR(50),
    FOREIGN KEY (BookID) REFERENCES Book(ID),
    FOREIGN KEY (ReaderID) REFERENCES Readers(ID)
);

SELECT * FROM Readers
WHERE ReaderID NOT IN (
    SELECT DISTINCT ReaderID FROM BookLoan
);

-- Задача 2:
SELECT Book.Title, Book.Price, Author.FirstName, Author.LastName
FROM Book
LEFT JOIN Author ON Book.ID = Author.ID;

-- Задача 3:
SELECT * FROM Book
FROM Book
WHERE Country = 'Bulgaria';

-- Задача 4:
SELECT DISTINCT Title
FROM Book
WHERE BookID IN (
    SELECT BookID
    FROM BookReader
    WHERE ReaderID IN (
        SELECT ReaderID
        FROM Readers
        WHERE Email LIKE '%@example.com'
    )
);

-- Задача 5:
SELECT FirstName, LastName
FROM Readers
WHERE ReaderID IN (
    SELECT ReaderID
    FROM BookReader
    WHERE DateTaken BETWEEN '2023-01-01' AND '2023-12-31'
);




